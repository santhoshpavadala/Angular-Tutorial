import { EmployeeOnboardModel } from './employee-onboard-model';

describe('EmployeeOnboardModel', () => {
  it('should create an instance', () => {
    expect(new EmployeeOnboardModel()).toBeTruthy();
  });
});
