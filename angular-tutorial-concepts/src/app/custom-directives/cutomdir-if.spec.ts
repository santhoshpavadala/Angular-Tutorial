import { CutomdirIf } from './cutomdir-if';

describe('CutomdirIf', () => {
  it('should create an instance', () => {
    const directive = new CutomdirIf();
    expect(directive).toBeTruthy();
  });
});
