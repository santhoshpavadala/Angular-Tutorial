import { CustomdirHighlite } from './customdir-highlite';

describe('CustomdirHighlite', () => {
  it('should create an instance', () => {
    const directive = new CustomdirHighlite();
    expect(directive).toBeTruthy();
  });
});
